{
  "create": {
    "requestBody": ["type", "title", "content", "city", "address", "time"]
  },
  "query": {
    "parameters": {},
    "service": "query",
    "options": {
      "query": ["skip", "limit"],
      "sort": ["meta.createAt"],
      "desc": true,
      "projection": {
        "content": 0
      }
    }
  },
  "simple": {
    "parameters": {},
    "service": "query",
    "options": {
      "query": ["skip", "limit"],
      "sort": ["meta.createAt"],
      "desc": true,
      "projection": {
        "tenant": 1,
        "title": 1,
        "meta.createdAt": 1
      }
    }
  },
  "fetch": {
    "query": ["_id"]
  }
}
